// Tamanho máximo do campo de observações da inspeção.
// Espelha a coluna INSPECAO.DES_OBSERVACOES (VARCHAR(255)) no backend —
// é aqui que o usuário anota a identificação do equipamento.
export const INSPECTION_NOTES_MAX_LENGTH = 255;
